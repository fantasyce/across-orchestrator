"""Compatibility namespace for the extracted Across Agents Assistant runtime.

Across Orchestrator keeps this namespace so the app's mature task-manager tests
can run unchanged while the public product APIs live under ``across_orchestrator``.
"""

