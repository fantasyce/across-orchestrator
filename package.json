{
  "name": "across-orchestrator",
  "private": true,
  "version": "0.7.8",
  "description": "Development-only browser probe dependencies for Across Orchestrator.",
  "scripts": {
    "test:browser-deps": "node -e \"require('playwright'); console.log('playwright ready')\""
  },
  "devDependencies": {
    "playwright": "^1.53.0"
  }
}
